import alpha from './alpha';
// Internal modules — a comment between imports must not break the run.
import beta from './beta';
import gamma from './gamma';

export const modules = [alpha, beta, gamma];
